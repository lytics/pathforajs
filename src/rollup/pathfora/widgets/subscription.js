/** @module pathfora/widgets/subscription */

import prepareWidget from '../../core/widgets/prepare-widget'

export default function Subscription (config) {
  return prepareWidget('subscription', config);
};